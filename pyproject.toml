[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "financial-analysis"
version = "0.1.0"
description = "Quickly estimate a company's 10-K at a very high level"
readme = "README.md"
requires-python = ">=3.12"
packages=["financial_analysis"]
dependencies = [
    "pydantic>=2.13.4",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.1",
    "ty>=0.0.69",
]
